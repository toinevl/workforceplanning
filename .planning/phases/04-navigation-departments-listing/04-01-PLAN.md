---
phase: 04-navigation-departments-listing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/domain.ts
  - src/lib/hooks/useDepartments.ts
  - src/components/layout/TopNav.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - LIST-03

must_haves:
  truths:
    - "The Departments link is visible in the top nav on every page"
    - "The Departments link is highlighted when the current URL starts with /departments"
    - "The Departments link is NOT highlighted on Home or Settings pages"
    - "DepartmentWithStats type exists and is exported from domain.ts"
    - "useDepartmentList returns DepartmentWithStats[] (not Department[])"
  artifacts:
    - path: "src/lib/types/domain.ts"
      provides: "DepartmentWithStats interface extending Department with headcount, totalFte, teamCount"
      contains: "DepartmentWithStats"
    - path: "src/lib/hooks/useDepartments.ts"
      provides: "useDepartmentList typed as useQuery<DepartmentWithStats[]>"
      contains: "DepartmentWithStats"
    - path: "src/components/layout/TopNav.tsx"
      provides: "Departments nav link with active detection"
      contains: "isDepartments"
  key_links:
    - from: "src/lib/hooks/useDepartments.ts"
      to: "src/lib/types/domain.ts"
      via: "import type { DepartmentWithStats }"
      pattern: "DepartmentWithStats"
    - from: "src/components/layout/TopNav.tsx"
      to: "/departments"
      via: "Link href"
      pattern: "href=\"/departments\""
---

<objective>
Fix the DepartmentWithStats type gap so the listing page can be typesafe, then add the Departments nav link with correct active-state detection.

Purpose: Establish the type foundation required by Plan 02's page, and deliver the navigation entry point (NAV-01, NAV-02). The type fix is here in Wave 1 so Plan 02 can import DepartmentWithStats without a circular dependency on the plan that creates the page.

Output: Three modified files — domain.ts (new DepartmentWithStats type), useDepartments.ts (type updated), TopNav.tsx (Departments link added).
</objective>

<execution_context>
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-01-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/toine/projects/workforceplanning/.planning/PROJECT.md
@/home/toine/projects/workforceplanning/.planning/ROADMAP.md
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-RESEARCH.md
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-PATTERNS.md
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs. Extracted from project source. -->

From src/lib/types/domain.ts (existing Department interface, lines 20-27):
```ts
export interface Department {
  id: string;
  name: string;
  description?: string;
  color: string;
  deptHead?: string;
  sortOrder: number;
}
```

From src/lib/hooks/useDepartments.ts (current hook to be updated, lines 1-12):
```ts
'use client';

import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
import type { Department } from '../types/domain';
import { fetchJSON } from '../utils/fetchJSON';

export function useDepartmentList() {
  return useQuery<Department[]>({
    queryKey: ['departments'],
    queryFn: () => fetchJSON('/api/departments'),
  });
}
```

From src/components/layout/TopNav.tsx (existing active-state pattern, lines 28-29 + 49-64):
```tsx
const isHome = pathname === '/' || pathname === '/scenarios';
const isSettings = pathname === '/settings';

// Existing link className (copy exactly):
"rounded px-2.5 py-2.5 text-sm font-medium text-gray-700 transition-colors hover:bg-gray-50 hover:text-gray-950 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gray-500 aria-[current=page]:bg-gray-100 aria-[current=page]:text-gray-950"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DepartmentWithStats to domain.ts and fix useDepartmentList type</name>
  <files>src/lib/types/domain.ts, src/lib/hooks/useDepartments.ts</files>
  <read_first>
    - src/lib/types/domain.ts — read the full file to find the exact line after the Department interface where DepartmentWithStats must be inserted; confirm no DepartmentWithStats already exists
    - src/lib/hooks/useDepartments.ts — read the full file to find the import line (line 4: `import type { Department }`) and the useDepartmentList function (lines 7-12) that needs the type updated
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-pathname.md — read per AGENTS.md requirement before touching Next.js files (this task touches a Next.js project hook)
  </read_first>
  <action>
    In src/lib/types/domain.ts: after the closing brace of the Department interface (at line 27 per PATTERNS.md), add a blank line then the DepartmentWithStats interface. The interface must extend Department and add exactly three fields: headcount (number), totalFte (number), teamCount (number). Export it. Do not modify the existing Department interface.

    In src/lib/hooks/useDepartments.ts: on the existing type import line (`import type { Department } from '../types/domain';`), add DepartmentWithStats to the named import: `import type { Department, DepartmentWithStats } from '../types/domain';`. Then in the useDepartmentList function body, change the generic type argument from `Department[]` to `DepartmentWithStats[]`. The queryKey and queryFn must remain unchanged: queryKey is ['departments'], queryFn calls fetchJSON('/api/departments'). No other changes to useDepartments.ts.
  </action>
  <verify>
    <automated>cd /home/toine/projects/workforceplanning && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/types/domain.ts contains `export interface DepartmentWithStats extends Department` with fields headcount: number, totalFte: number, teamCount: number
    - src/lib/hooks/useDepartments.ts imports `DepartmentWithStats` from '../types/domain'
    - src/lib/hooks/useDepartments.ts useDepartmentList function uses `useQuery<DepartmentWithStats[]>` (not `useQuery<Department[]>`)
    - `tsc --noEmit` exits 0 (no TypeScript errors introduced)
    - The Department interface in domain.ts is unchanged (same fields, same line range)
  </acceptance_criteria>
  <done>DepartmentWithStats type exported from domain.ts; useDepartmentList returns DepartmentWithStats[]; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Departments nav link to TopNav.tsx</name>
  <files>src/components/layout/TopNav.tsx</files>
  <read_first>
    - src/components/layout/TopNav.tsx — read the full file to confirm: exact line numbers of isHome/isSettings declarations (lines 28-29), exact className string on existing links (lines 53-63), the exact insertion point inside the `flex items-center gap-1` div
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/link.md — read per AGENTS.md requirement before writing any Link usage
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-pathname.md — confirm usePathname API (already imported in TopNav; verify import exists)
  </read_first>
  <action>
    In src/components/layout/TopNav.tsx:

    1. After the isSettings declaration (line 29 per PATTERNS.md), add: `const isDepartments = pathname.startsWith('/departments');`. Use startsWith — NOT pathname === '/departments'. startsWith is required so the active state persists when navigating to /departments/[deptId] detail pages in Phase 5.

    2. Inside the existing `<div className="flex items-center gap-1">` block (per PATTERNS.md lines 40-63), add a new Link element after the existing Settings link. The new Link must have: href="/departments", aria-current={isDepartments ? 'page' : undefined}, the exact same className as the Settings link (copy character-for-character: "rounded px-2.5 py-2.5 text-sm font-medium text-gray-700 transition-colors hover:bg-gray-50 hover:text-gray-950 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-gray-500 aria-[current=page]:bg-gray-100 aria-[current=page]:text-gray-950"), and the text content "Departments". No wrapping anchor tag — per Next.js 16 docs, Link is the anchor itself; do not nest an `<a>` inside Link.

    The existing Home and Settings links must not be modified.
  </action>
  <verify>
    <automated>cd /home/toine/projects/workforceplanning && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/TopNav.tsx contains `const isDepartments = pathname.startsWith('/departments');`
    - src/components/layout/TopNav.tsx contains `href="/departments"`
    - src/components/layout/TopNav.tsx contains `aria-current={isDepartments ? 'page' : undefined}`
    - The Departments Link has className containing `aria-[current=page]:bg-gray-100` and `aria-[current=page]:text-gray-950`
    - The text content "Departments" is present inside the Link element
    - isHome and isSettings declarations are unchanged (grep for both — they must still exist)
    - `tsc --noEmit` exits 0
    - grep -c 'isDepartments' src/components/layout/TopNav.tsx returns at least 2 (declaration + usage in aria-current)
  </acceptance_criteria>
  <done>Departments link visible in top nav; active state uses startsWith pattern; TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Frontend | dept.name, dept.color values from Azure Table Storage rendered in JSX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | TopNav.tsx — dept.name rendered as JSX text | accept | React JSX auto-escapes string children; no dangerouslySetInnerHTML used |
| T-04-02 | Tampering | DepartmentCard — style={{ backgroundColor: dept.color }} | accept | Inline style scoped to element; CSS color values have no script execution vector; no innerHTML involved |
| T-04-03 | Information Disclosure | /departments route — lists all department names | accept | Single-user app with no auth in v2.0 per REQUIREMENTS.md out-of-scope; accepted limitation |

No package installs in this plan — slopcheck row not applicable.
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors after both tasks complete
2. `grep -n 'DepartmentWithStats' src/lib/types/domain.ts` returns a line with `export interface DepartmentWithStats extends Department`
3. `grep -n 'DepartmentWithStats' src/lib/hooks/useDepartments.ts` returns at least two lines (import + generic usage)
4. `grep -n 'isDepartments' src/components/layout/TopNav.tsx` returns at least two lines
5. `grep -n "startsWith('/departments')" src/components/layout/TopNav.tsx` returns exactly one line
6. `grep -n 'href="/departments"' src/components/layout/TopNav.tsx` returns exactly one line
</verification>

<success_criteria>
- DepartmentWithStats interface exported from src/lib/types/domain.ts with fields: headcount: number, totalFte: number, teamCount: number
- useDepartmentList hook returns useQuery typed as DepartmentWithStats[] (not Department[])
- TopNav.tsx contains isDepartments using startsWith('/departments') and a Link to /departments with aria-current active pattern
- tsc --noEmit exits 0 (no compile errors)
</success_criteria>

<output>
Create `.planning/phases/04-navigation-departments-listing/04-01-SUMMARY.md` when done.
</output>
