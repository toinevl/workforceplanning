---
phase: 04-navigation-departments-listing
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/departments/page.tsx
  - src/components/departments/DepartmentCard.tsx
autonomous: true
requirements:
  - LIST-01
  - LIST-02
  - LIST-03
  - LIST-04

must_haves:
  truths:
    - "Navigating to /departments shows a list of department cards"
    - "Each card displays department name, color badge, headcount, total FTE, and team count"
    - "Teams with no departmentId appear in a visually distinct Unassigned card (dashed border, grey badge)"
    - "The page shows 3 animate-pulse skeleton cards while data is loading"
    - "The page shows an error message with the error text when the fetch fails"
    - "When no departments and no unassigned teams exist, the empty state message is shown"
    - "FTE values are displayed with exactly one decimal place (e.g. 8.5 FTE, not 8.500000000000002 FTE)"
    - "No per-card queries — the page makes exactly one useQuery call (useDepartmentList)"
  artifacts:
    - path: "src/app/departments/page.tsx"
      provides: "Client Component listing page wrapping AppShell"
      contains: "'use client'"
    - path: "src/components/departments/DepartmentCard.tsx"
      provides: "Display-only card with color badge and stat chips"
      contains: "DepartmentWithStats"
  key_links:
    - from: "src/app/departments/page.tsx"
      to: "src/lib/hooks/useDepartments.ts"
      via: "useDepartmentList import"
      pattern: "useDepartmentList"
    - from: "src/app/departments/page.tsx"
      to: "src/components/departments/DepartmentCard.tsx"
      via: "DepartmentCard import"
      pattern: "DepartmentCard"
    - from: "src/components/departments/DepartmentCard.tsx"
      to: "src/lib/types/domain.ts"
      via: "import type { DepartmentWithStats }"
      pattern: "DepartmentWithStats"
---

<objective>
Create the /departments listing page and DepartmentCard component, delivering the full departments listing feature with loading, error, empty, and Unassigned-bucket states.

Purpose: This is the visible deliverable of Phase 4 — users can navigate to /departments and see all departments with rollup stats at a glance (LIST-01, LIST-02, LIST-03, LIST-04).

Output: Two new files — src/app/departments/page.tsx (listing page) and src/components/departments/DepartmentCard.tsx (display card component).
</objective>

<execution_context>
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-02-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/toine/projects/workforceplanning/.planning/PROJECT.md
@/home/toine/projects/workforceplanning/.planning/ROADMAP.md
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-RESEARCH.md
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-PATTERNS.md
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-UI-SPEC.md
@/home/toine/projects/workforceplanning/.planning/phases/04-navigation-departments-listing/04-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Available after Plan 01 executes. -->

From src/lib/types/domain.ts (added in Plan 01):
```ts
export interface DepartmentWithStats extends Department {
  headcount: number;
  totalFte: number;
  teamCount: number;
}
// Department base type (pre-existing):
export interface Department {
  id: string;
  name: string;
  description?: string;
  color: string;
  deptHead?: string;
  sortOrder: number;
}
```

From src/lib/hooks/useDepartments.ts (updated in Plan 01):
```ts
export function useDepartmentList(): UseQueryResult<DepartmentWithStats[]>
// queryKey: ['departments']
// queryFn: fetchJSON('/api/departments')
// Unassigned bucket: API appends { id: 'unassigned', name: 'Unassigned', color: '', headcount: N, totalFte: N, teamCount: N } when unassigned teams exist
```

From src/app/settings/page.tsx (analog — AppShell page pattern):
```tsx
'use client';
import { AppShell } from '@/components/layout/AppShell';
// Page structure: <AppShell><div className="max-w-6xl mx-auto py-6 px-4">...</div></AppShell>
```

From src/components/departments/DepartmentsSection.tsx (loading/error patterns):
```tsx
// Loading skeleton (lines 266-270):
{[...Array(3)].map((_, i) => (
  <div key={i} className="h-16 w-full animate-pulse rounded-lg bg-gray-100" />
))}
// Error state (lines 257-264):
<div className="rounded-lg border border-red-200 bg-red-50 px-4 py-3">
  <p className="text-sm text-red-700">
    Failed to load departments.{' '}{listQuery.error instanceof Error ? listQuery.error.message : ''}
  </p>
</div>
// Color badge (lines 43-47):
<span className="mt-1 h-4 w-4 flex-shrink-0 rounded-full border border-gray-200" style={{ backgroundColor: dept.color }} aria-hidden="true" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DepartmentCard component</name>
  <files>src/components/departments/DepartmentCard.tsx</files>
  <read_first>
    - src/components/departments/DepartmentsSection.tsx — read to extract exact color badge markup (lines 43-47), card container classes (line 41), name/description block (lines 48-55). Do not copy the edit/delete action buttons — DepartmentCard in Phase 4 is display-only.
    - src/components/layout/TopNav.tsx (lines 43-46) — read for the version badge stat chip pattern used as the model for headcount/FTE/team-count chips
    - src/lib/types/domain.ts — confirm DepartmentWithStats is now present (written in Plan 01 Task 1); read the exact field names before writing the component
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/link.md — read per AGENTS.md requirement (even though this component has no Link in Phase 4, it is a Next.js project file)
  </read_first>
  <action>
    Create src/components/departments/DepartmentCard.tsx as a React component file. Start with `'use client';` directive.

    Import: `import type { DepartmentWithStats } from '@/lib/types/domain';`. No other imports needed — no hooks, no Link (display-only in Phase 4 per RESEARCH.md Open Questions §2).

    Define props interface: `interface DepartmentCardProps { dept: DepartmentWithStats; }` and export the named function `export function DepartmentCard({ dept }: DepartmentCardProps)`.

    Inside the component, detect the Unassigned bucket by checking `dept.id === 'unassigned'`. Use this boolean to drive two visual differences:

    Card container classes:
    - Regular dept: `"flex items-center gap-4 rounded-lg border border-gray-200 bg-white p-4"`
    - Unassigned dept: `"flex items-center gap-4 rounded-lg border border-dashed border-gray-300 bg-gray-50 p-4"`

    Color badge:
    - Regular dept: `<span className="h-4 w-4 flex-shrink-0 rounded-full border border-gray-200" style={{ backgroundColor: dept.color }} aria-hidden="true" />`
    - Unassigned dept: `<span className="h-4 w-4 flex-shrink-0 rounded-full border border-dashed border-gray-400 bg-gray-300" aria-hidden="true" />`

    Name block (flex-1 div):
    - Regular dept: `<p className="text-sm font-semibold text-gray-900">{dept.name}</p>`
    - Unassigned dept: `<p className="text-sm italic text-gray-500">Unassigned</p>` (ignore dept.name for the Unassigned bucket; use the literal "Unassigned" per UI-SPEC copywriting contract)
    - Show description only for regular depts: `{dept.description && <p className="mt-0.5 text-xs text-gray-500">{dept.description}</p>}`

    Stat chips row (flex items-center gap-2 flex-shrink-0 div):
    - Headcount chip: `<span className="rounded border border-gray-200 bg-gray-50 px-1.5 py-0.5 text-xs font-medium text-gray-700">{dept.headcount} people</span>`
    - FTE chip: `<span className="rounded border border-gray-200 bg-gray-50 px-1.5 py-0.5 text-xs font-medium text-gray-700">{dept.totalFte.toFixed(1)} FTE</span>` — toFixed(1) is mandatory to prevent IEEE 754 float display bugs
    - Team count chip: `<span className="rounded border border-gray-200 bg-gray-50 px-1.5 py-0.5 text-xs font-medium text-gray-700">{dept.teamCount} {dept.teamCount === 1 ? 'team' : 'teams'}</span>` — pluralise per UI-SPEC copywriting contract

    Do not add any onClick, Link wrapper, or hover-lift effects — Phase 4 cards are display-only.
  </action>
  <verify>
    <automated>cd /home/toine/projects/workforceplanning && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/departments/DepartmentCard.tsx exists
    - File starts with `'use client';`
    - Imports `DepartmentWithStats` from `@/lib/types/domain`
    - Contains `dept.id === 'unassigned'` conditional (Unassigned bucket detection)
    - Contains `border-dashed` class (Unassigned container styling)
    - Contains `dept.totalFte.toFixed(1)` (float guard per RESEARCH.md Pitfall 5)
    - Contains `dept.teamCount === 1 ? 'team' : 'teams'` (plural per UI-SPEC)
    - Contains `aria-hidden="true"` on color badge span
    - Does NOT contain `useQuery`, `useDepartmentList`, or any hook call (display-only component)
    - Does NOT contain `href` or `Link` (no navigation in Phase 4)
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>DepartmentCard renders color badge, name, and three stat chips; distinguishes Unassigned bucket visually; toFixed(1) on FTE; TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create /departments listing page</name>
  <files>src/app/departments/page.tsx</files>
  <read_first>
    - src/app/settings/page.tsx — read the full file for the AppShell wrapper pattern, page layout classes, and the 'use client' + import pattern to replicate exactly
    - src/components/departments/DepartmentsSection.tsx — read lines 257-275 for the exact isLoading/isError block pattern to copy
    - src/components/departments/DepartmentCard.tsx — confirm the file exists (written in Task 1 above) and the named export is `DepartmentCard`
    - node_modules/next/dist/docs/01-app/03-api-reference/02-components/link.md — read per AGENTS.md requirement before writing any Next.js page file
    - node_modules/next/dist/docs/01-app/01-getting-started/01-installation.md — read for any breaking changes relevant to page.tsx files in this version
  </read_first>
  <action>
    Create src/app/departments/page.tsx. The parent directory src/app/departments/ may need to be created if it does not exist — use mkdir -p via the shell before writing the file if needed. The file must be a Client Component because it calls useDepartmentList (a TanStack Query hook).

    File structure:
    - Start with `'use client';` directive (required — TanStack Query hooks cannot run in Server Components)
    - Imports: `AppShell` from `@/components/layout/AppShell`, `useDepartmentList` from `@/lib/hooks/useDepartments`, `DepartmentCard` from `@/components/departments/DepartmentCard`
    - Export default function named `DepartmentsPage`

    Inside the function:
    - Call `const listQuery = useDepartmentList();` — this is the single data fetch for the entire page; do not add any other useQuery calls
    - Return JSX wrapping everything in `<AppShell>` with no board prop
    - Inner layout div: `<div className="max-w-6xl mx-auto py-6 px-4">`
    - Page heading: `<h1 className="text-2xl font-bold text-gray-900">Departments</h1>`
    - Page subheading: `<p className="mt-1 text-sm text-gray-600">All departments and rollup stats.</p>`

    Conditional rendering (in order — isLoading first, then isError, then data):

    Loading state (when `listQuery.isLoading`): render a div with `className="mt-6 flex flex-col gap-3"` containing 3 skeleton divs: `[...Array(3)].map((_, i) => <div key={i} className="h-16 w-full animate-pulse rounded-lg bg-gray-100" />)`. No text in loading state.

    Error state (when `listQuery.isError`): render `<div className="mt-6 rounded-lg border border-red-200 bg-red-50 px-4 py-3"><p className="text-sm text-red-700">Failed to load departments.{' '}{listQuery.error instanceof Error ? listQuery.error.message : ''}</p></div>`

    Data state (when neither loading nor error): render a `<div className="mt-6 flex flex-col gap-4">` containing `(listQuery.data ?? []).map((dept) => <DepartmentCard key={dept.id} dept={dept} />)`. When `listQuery.data` is an empty array `[]`, also render the empty state: `<p className="mt-6 text-sm text-gray-500">No departments yet. Go to Settings to create the first one.</p>`. Place the empty state paragraph inside the map container as a conditional — check `listQuery.data?.length === 0` for the empty state, not `listQuery.data === undefined` (undefined means still loading).

    Implement the three rendering states as mutually exclusive conditional blocks: `{listQuery.isLoading && (...)}`, `{listQuery.isError && (...)}`, `{!listQuery.isLoading && !listQuery.isError && (...)}`. This is the established pattern from DepartmentsSection.tsx.
  </action>
  <verify>
    <automated>cd /home/toine/projects/workforceplanning && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/departments/page.tsx exists
    - File starts with `'use client';`
    - Contains `import { AppShell }` from '@/components/layout/AppShell'
    - Contains `import { useDepartmentList }` from '@/lib/hooks/useDepartments'
    - Contains `import { DepartmentCard }` from '@/components/departments/DepartmentCard'
    - Contains exactly one call to `useDepartmentList()` (no other useQuery or useDepartmentList calls)
    - Does NOT contain `useDepartment(` (no per-card queries)
    - Contains `listQuery.isLoading` conditional
    - Contains `listQuery.isError` conditional
    - Contains `animate-pulse` class (loading skeleton)
    - Contains `border-red-200` class (error state)
    - Contains `Failed to load departments` text
    - Contains `No departments yet` text (empty state)
    - Contains `DepartmentCard` JSX usage with `key={dept.id}` and `dept={dept}` props
    - Contains `<AppShell>` wrapper with no board prop
    - `tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/departments page renders department cards from useDepartmentList; shows loading/error/empty states; single fetch; TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Frontend | dept.name, dept.color, dept.description, dept.deptHead values from Azure Table Storage rendered in JSX |
| Error → Frontend | Error.message from failed fetch rendered in error state div |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | DepartmentCard — dept.name, dept.description rendered as JSX text | accept | React JSX auto-escapes string children; no dangerouslySetInnerHTML anywhere in this plan |
| T-04-05 | Tampering | DepartmentCard — style={{ backgroundColor: dept.color }} | accept | CSS color values have no script execution vector; inline style is element-scoped; no eval path |
| T-04-06 | Information Disclosure | listQuery.error.message rendered in error state | accept | Error messages originate from the app's own fetch layer (not external attacker-controlled); no PII in dept API errors; single-user app with no auth in v2.0 |
| T-04-07 | Information Disclosure | /departments/page.tsx — all department names visible | accept | Single-user app, no auth in v2.0 per REQUIREMENTS.md out-of-scope; accepted known limitation |

No package installs in this plan — slopcheck row not applicable.
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0 after both tasks complete
2. `grep -n "'use client'" src/app/departments/page.tsx` returns line 1
3. `grep -c 'useDepartmentList' src/app/departments/page.tsx` returns 2 (import + one call)
4. `grep -c 'useDepartment(' src/app/departments/page.tsx` returns 0 (no per-card queries)
5. `grep -n 'animate-pulse' src/app/departments/page.tsx` returns at least one line
6. `grep -n 'border-red-200' src/app/departments/page.tsx` returns at least one line
7. `grep -n "toFixed(1)" src/components/departments/DepartmentCard.tsx` returns at least one line
8. `grep -n "dept.id === 'unassigned'" src/components/departments/DepartmentCard.tsx` returns at least one line
9. `grep -n 'border-dashed' src/components/departments/DepartmentCard.tsx` returns at least one line
</verification>

<success_criteria>
- src/app/departments/page.tsx exists as 'use client' Client Component, wraps AppShell, calls useDepartmentList once, renders DepartmentCard per department, shows loading/error/empty states
- src/components/departments/DepartmentCard.tsx exists, renders color badge + name + three stat chips, distinguishes Unassigned bucket with dashed border and grey badge, uses toFixed(1) for FTE, pluralises team count
- No per-card queries anywhere in either file
- tsc --noEmit exits 0
</success_criteria>

<output>
Create `.planning/phases/04-navigation-departments-listing/04-02-SUMMARY.md` when done.
</output>
