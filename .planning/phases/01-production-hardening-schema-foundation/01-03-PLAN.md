---
phase: 01-production-hardening-schema-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/db/client.ts
  - src/lib/db/seed.ts
autonomous: true
requirements: [HARD-01, HARD-03, SEED-01]
must_haves:
  truths:
    - Seed resetFirst is blocked when NODE_ENV is 'production' OR connection string is not 'UseDevelopmentStorage=true'
    - resetFirst returns a clear error message without deleting any data
    - Department migration uses a sentinel row pattern (check before inserting)
    - Departments table is initialized in ensureTablesExist()
    - Seed creates 2-3 sample departments and assigns teams to them
  artifacts:
    - path: src/lib/db/client.ts
      provides: ensureTablesExist includes 'departments' table
      pattern: "'departments'"
    - path: src/lib/db/seed.ts
      provides: Production guard check + department creation in runSeed
      pattern: "NODE_ENV|resetFirst"
  key_links:
    - from: src/lib/db/client.ts
      to: src/lib/db/seed.ts
      via: ensureTablesExist call
      pattern: "ensureTablesExist\\(\\)"
    - from: src/lib/db/seed.ts
      to: new departments table
      via: creating sample departments
      pattern: "departmentId|TABLE_DEPARTMENTS"
---

<objective>
Guard production data integrity and initialize the departments table on startup. Add production-safety check to seed panel's resetFirst operation and implement idempotent department migration. Update seed data to create sample departments and assign teams to them.

Purpose: Prevent accidental data loss in production; establish migration safety; enable multi-department dev setup from first run
Output: Production-safe seed with departments table in ensureTablesExist; sample departments in seed data
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@/home/toine/projects/workforceplanning/.planning/research/PITFALLS.md (Pitfalls 1, 2)
@/home/toine/projects/workforceplanning/.planning/research/STACK.md (Default Department spec)
</execution_context>

<context>
@/home/toine/projects/workforceplanning/src/lib/db/client.ts
@/home/toine/projects/workforceplanning/src/lib/db/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'departments' to ensureTablesExist()</name>
  <files>src/lib/db/client.ts</files>
  <read_first>
    - src/lib/db/client.ts (lines 38-52: ensureTablesExist function and tables array)
  </read_first>
  <action>
    Add 'departments' to the tables array inside ensureTablesExist() at line 40, as the first entry (before 'teams'). This ensures departments table is created before any team operations.
  </action>
  <verify>
    <automated>grep -A 10 "const tables = " src/lib/db/client.ts | grep -c "departments"</automated>
  </verify>
  <done>
    - 'departments' is in the tables array passed to Promise.all
    - It appears before 'teams' to establish ordering dependency
    - No other changes to ensureTablesExist
  </done>
</task>

<task type="auto">
  <name>Task 2: Add production guard to seed resetFirst operation</name>
  <files>src/lib/db/seed.ts</files>
  <read_first>
    - src/lib/db/seed.ts (lines 214-235: runSeed function and resetFirst block)
    - src/lib/db/client.ts (lines 5-6: getConnectionString function to understand what constitutes a dev connection)
  </read_first>
  <action>
    In runSeed function, before the resetFirst block (before line 226), add a guard check. The guard must:
    
    1. Check if options.resetFirst is true
    2. If true, verify BOTH conditions are safe (this is a dev environment):
       - process.env.NODE_ENV !== 'production' AND
       - connection string includes 'UseDevelopmentStorage' (call getConnectionString() to check)
    3. If EITHER condition indicates production (NODE_ENV === 'production' OR connection string is NOT 'UseDevelopmentStorage=true'), throw an error with message: "Cannot reset seed on production connection string. This would delete all live teams data."
    4. If conditions pass (safe to delete), proceed to the existing deleteByPartitionKey calls
    
    Import getConnectionString from './client' at the top of the file if not already imported.
  </action>
  <verify>
    <automated>grep -n "NODE_ENV.*production\|UseDevelopmentStorage" src/lib/db/seed.ts</automated>
  </verify>
  <done>
    - resetFirst operation checks NODE_ENV before destructive delete
    - resetFirst operation checks connection string (must contain 'UseDevelopmentStorage')
    - Error message is clear: "Cannot reset seed on production connection string. This would delete all live teams data."
    - Guard is evaluated BEFORE any deleteByPartitionKey calls
    - Dev environment passes both checks and proceeds normally
  </done>
</task>

<task type="auto">
  <name>Task 3: Add sample departments to seed and assign teams to them</name>
  <files>src/lib/db/seed.ts</files>
  <read_first>
    - src/lib/db/seed.ts (lines 1-10: imports; lines 23-30: TEAMS array structure; lines 214-298: runSeed function body including team insert loop at line 239)
  </read_first>
  <action>
    Before the team insert loop (around line 239), add department creation. Create 3 sample departments:
    
    1. "Engineering" — color: '#6366f1' (indigo), sortOrder: 0
    2. "Product" — color: '#3b82f6' (blue), sortOrder: 1
    3. "Operations" — color: '#22c55e' (green), sortOrder: 2
    
    Store their rowKeys in a map (e.g., departmentMap) for use during team insertion.
    
    Insert departments using departmentClient.upsertEntity with UpdateMode 'Replace'. Use v4 uuid for rowKey.
    
    During team insertion (in the existing for...of loop at line 239), assign teams to departments in a round-robin pattern:
    - Team Alpha → Engineering
    - Team Bravo → Product
    - Team Charlie → Operations
    - Team Delta → Engineering
    - Team Echo → Product
    - Team Foxtrot → Operations
    
    Add departmentId field to the TeamEntity upsert at line 244.
    
    Get TABLE_DEPARTMENTS constant if needed: import { TABLE_DEPARTMENTS } from './tables' (already imported at line 3).
  </action>
  <verify>
    <automated>grep -c "partitionKey: 'department'" src/lib/db/seed.ts && grep -c "departmentId:" src/lib/db/seed.ts</automated>
  </verify>
  <done>
    - 3 sample departments are created in seed (Engineering, Product, Operations)
    - Each department has a unique uuid rowKey, name, color, and sortOrder
    - Departments are inserted via departmentClient.upsertEntity before team insertion
    - Teams are assigned to departments via departmentId field in TeamEntity upsert
    - Round-robin assignment: teams distributed across 3 departments
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement idempotent migration sentinel pattern (HARD-03)</name>
  <files>src/lib/db/seed.ts</files>
  <read_first>
    - src/lib/db/seed.ts (lines 214-235: resetFirst block; lines 237-251: team insertion loop)
    - STACK.md section 4 (migration sentinel design and UpdateMode.Merge usage)
  </read_first>
  <action>
    After team insertion but before member insertion, add an idempotent migration check for the default department. This check prevents duplicate "Default" departments if migration runs multiple times.
    
    Create a helper function ensureDefaultDepartment() that:
    1. Attempts to read from departmentClient with filter for rowKey 'default' or partitionKey 'migration'
    2. If a department with rowKey 'default' exists, skip creation (idempotent)
    3. If none exists, create one: rowKey 'default', name 'Default', color '#6b7280', sortOrder 99, with createdAt/updatedAt
    
    Actually: SIMPLER approach per STACK.md — just try to create via upsertEntity with UpdateMode 'Replace' and catch any error. The upsert pattern is idempotent by design.
    
    After the team insertion loop, call ensureDefaultDepartment() before member insertion. This establishes a known default department for later migration endpoint (Phase 2) to reference.
    
    Document the purpose: "Ensure default department exists for team migration in Phase 2."
  </action>
  <verify>
    <automated>grep -n "DEFAULT.*DEPARTMENT\|rowKey.*default" src/lib/db/seed.ts</automated>
  </verify>
  <done>
    - Default department is created (or verified to exist) during seed with rowKey 'default'
    - Default department creation is idempotent (safe to run multiple times)
    - Default department has consistent structure for Phase 2 migration endpoint to find
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production detection | NODE_ENV check + connection string check |
| Data mutation | resetFirst destructive operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Seed resetFirst on production | mitigate | Double-check guard: NODE_ENV and connection string both must indicate dev (not production) before allowing delete. Error message is clear and non-recoverable (throw, no silent skip). |
| T-01-06 | Tampering | Migration duplicate departments | mitigate | Sentinel/idempotent pattern: upsertEntity with fixed rowKey 'default' ensures one-shot creation; running migration twice creates same entity, not duplicates. |
| T-01-07 | Denial of Service | Production deletion guard false negative | mitigate | Both NODE_ENV and connection string checked; only one needs to be correct. False positive (blocks when shouldn't) is preferable to false negative (allows deletion in prod). |
</threat_model>

<verification>
After Wave 3 execution:
- Local dev seed with default storage creates sample departments and assigns teams
- Running seed twice does not create duplicate departments
- Production environment with real connection string rejects resetFirst with clear error message
- Departments table exists and is initialized on app startup
</verification>

<success_criteria>
- ensureTablesExist() includes 'departments' in the tables array
- runSeed() throws error if resetFirst is true AND (NODE_ENV === 'production' OR connection string is not 'UseDevelopmentStorage=true')
- Sample departments (Engineering, Product, Operations) are created during seed with correct colors and sortOrder
- Teams are assigned to sample departments via departmentId field
- Default department with rowKey 'default' is created idempotently
- Seed can be run multiple times without creating duplicate departments
</success_criteria>

<output>
Create `.planning/phases/01-production-hardening-schema-foundation/01-03-SUMMARY.md` when done
</output>
