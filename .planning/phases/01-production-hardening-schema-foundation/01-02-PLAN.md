---
phase: 01-production-hardening-schema-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/lib/db/mappers.ts
  - src/lib/api/teams.ts
  - src/lib/api/scenarios.ts
autonomous: true
requirements: [HARD-02]
must_haves:
  truths:
    - entityToTeam() function is defined in a single location (mappers.ts) that both teams.ts and scenarios.ts import from
    - entityToTeam() correctly maps departmentId from TeamEntity to Team domain type
    - No duplicate entityToTeam() function remains in scenarios.ts
  artifacts:
    - path: src/lib/db/mappers.ts
      provides: Single shared entityToTeam() function
      exports: ["export function entityToTeam"]
    - path: src/lib/api/teams.ts
      provides: getAllTeams uses imported entityToTeam from mappers
      pattern: "import.*entityToTeam.*from.*mappers"
    - path: src/lib/api/scenarios.ts
      provides: getScenarioBoardState uses imported entityToTeam from mappers
      pattern: "import.*entityToTeam.*from.*mappers"
  key_links:
    - from: src/lib/db/mappers.ts
      to: src/lib/api/teams.ts
      via: import statement
      pattern: "from .*mappers"
    - from: src/lib/db/mappers.ts
      to: src/lib/api/scenarios.ts
      via: import statement
      pattern: "from .*mappers"
---

<objective>
Consolidate the entityToTeam() mapper into a single shared location so that schema changes (like adding departmentId) propagate to all consumers without duplication risk. This prevents silent bugs where the board returns teams missing the new field.

Purpose: Ensure all mappers stay in sync as types evolve
Output: New src/lib/db/mappers.ts with entityToTeam(); both teams.ts and scenarios.ts import from it
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@/home/toine/projects/workforceplanning/.planning/research/PITFALLS.md (Pitfall 3)
</execution_context>

<context>
@/home/toine/projects/workforceplanning/src/lib/api/teams.ts
@/home/toine/projects/workforceplanning/src/lib/api/scenarios.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/lib/db/mappers.ts with consolidated entityToTeam()</name>
  <files>src/lib/db/mappers.ts</files>
  <read_first>
    - src/lib/api/teams.ts (lines 5-12: the entityToTeam function to extract)
  </read_first>
  <action>
    Create new file src/lib/db/mappers.ts. Copy entityToTeam() function from teams.ts as-is (it already includes departmentId mapping from Wave 1 types). Import types needed: type { TeamEntity } from './tables'; type { Team } from '../types/domain';
    
    Export the function: export function entityToTeam(e: TeamEntity): Team { ... }
  </action>
  <verify>
    <automated>test -f src/lib/db/mappers.ts && grep -c "export function entityToTeam" src/lib/db/mappers.ts</automated>
  </verify>
  <done>
    - src/lib/db/mappers.ts exists
    - entityToTeam() function is exported
    - Function maps all Team fields including departmentId from TeamEntity
  </done>
</task>

<task type="auto">
  <name>Task 2: Update src/lib/api/teams.ts to import entityToTeam from mappers</name>
  <files>src/lib/api/teams.ts</files>
  <read_first>
    - src/lib/api/teams.ts (entire file: see current entityToTeam at line 5, its usage in getAllTeams at line 19, getTeam at line 28)
  </read_first>
  <action>
    Remove the entityToTeam function definition (lines 5-13) from teams.ts.
    
    Add import statement at the top: import { entityToTeam } from '../db/mappers';
    
    Existing usages of entityToTeam in getAllTeams and getTeam remain unchanged and now use the imported version.
  </action>
  <verify>
    <automated>grep -c "import.*entityToTeam.*from.*mappers" src/lib/api/teams.ts && grep -c "^function entityToTeam" src/lib/api/teams.ts || echo "0"</automated>
  </verify>
  <done>
    - entityToTeam is imported from '../db/mappers' at the top of teams.ts
    - No duplicate entityToTeam function definition remains in teams.ts
    - getAllTeams and getTeam continue to use entityToTeam without change
  </done>
</task>

<task type="auto">
  <name>Task 3: Check and update src/lib/api/scenarios.ts to import entityToTeam from mappers</name>
  <files>src/lib/api/scenarios.ts</files>
  <read_first>
    - src/lib/api/scenarios.ts (search for entityToTeam function: expect it around line 40-50; check for usage in getScenarioBoardState)
  </read_first>
  <action>
    Find the entityToTeam function definition in scenarios.ts. Remove it.
    
    Add import statement at the top: import { entityToTeam } from '../db/mappers';
    
    Verify that getScenarioBoardState and any other functions using entityToTeam continue to work with the imported version.
  </action>
  <verify>
    <automated>grep -c "import.*entityToTeam.*from.*mappers" src/lib/api/scenarios.ts && grep -c "^function entityToTeam" src/lib/api/scenarios.ts || echo "0"</automated>
  </verify>
  <done>
    - entityToTeam is imported from '../db/mappers' at the top of scenarios.ts
    - No duplicate entityToTeam function definition remains in scenarios.ts
    - getScenarioBoardState continues to use entityToTeam without change
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Entity to Domain conversion | entityToTeam mapping layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | entityToTeam mapper consolidation | mitigate | Single function definition prevents silent divergence; TypeScript compilation ensures correct field names at build time |
</threat_model>

<verification>
After Wave 2 execution:
- TypeScript compilation succeeds
- All imports resolve correctly
- No entityToTeam duplicate definitions remain
</verification>

<success_criteria>
- src/lib/db/mappers.ts exists and exports entityToTeam function
- entityToTeam correctly maps departmentId field from entity to domain
- teams.ts imports entityToTeam from '../db/mappers' and no longer defines it locally
- scenarios.ts imports entityToTeam from '../db/mappers' and no longer defines it locally
- No other code changes; getAllTeams, getTeam, getScenarioBoardState behavior is unchanged
</success_criteria>

<output>
Create `.planning/phases/01-production-hardening-schema-foundation/01-02-SUMMARY.md` when done
</output>
