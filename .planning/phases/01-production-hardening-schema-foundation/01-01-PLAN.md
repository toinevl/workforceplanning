---
phase: 01-production-hardening-schema-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/tables.ts
  - src/lib/types/domain.ts
autonomous: true
requirements: [DEPT-05]
must_haves:
  truths:
    - DepartmentEntity type is defined with all required fields (name, color, description, deptHead, sortOrder, createdAt, updatedAt)
    - TABLE_DEPARTMENTS constant is exported from tables.ts
    - Team domain type includes optional departmentId field
    - TeamEntity includes optional departmentId field
  artifacts:
    - path: src/lib/db/tables.ts
      provides: DepartmentEntity interface + TABLE_DEPARTMENTS constant
      contains: "export interface DepartmentEntity"
    - path: src/lib/types/domain.ts
      provides: Department type + departmentId on Team
      contains: "departmentId?: string"
  key_links:
    - from: DepartmentEntity in tables.ts
      to: Department type in domain.ts
      via: matching field names and structure
      pattern: "name, description?, color, deptHead?"
---

<objective>
Define the Department entity schema and update Team types to include optional department reference. These type definitions are the foundation for all subsequent schema and API work.

Purpose: Establish type contracts that will be consumed by mappers, API routes, and UI components.
Output: Updated tables.ts with DepartmentEntity and TABLE_DEPARTMENTS; updated domain.ts with Department type and departmentId on Team
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@/home/toine/projects/workforceplanning/.planning/research/ARCHITECTURE.md
</execution_context>

<context>
@/home/toine/projects/workforceplanning/src/lib/db/tables.ts
@/home/toine/projects/workforceplanning/src/lib/types/domain.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DepartmentEntity to tables.ts and TABLE_DEPARTMENTS constant</name>
  <files>src/lib/db/tables.ts</files>
  <read_first>
    - src/lib/db/tables.ts (read existing entity patterns: TeamEntity at line 14, StaffMemberEntity at line 23)
    - ARCHITECTURE.md section 1a (DepartmentEntity schema spec)
  </read_first>
  <action>
    Add TABLE_DEPARTMENTS constant after the existing table name constants (after TABLE_AUDIT_EVENTS).
    
    Add DepartmentEntity interface after StaffMemberEntity (around line 37). Match the existing pattern:
    - partitionKey: 'department' (fixed literal, like 'team')
    - rowKey: string (departmentId uuid)
    - Fields: name (string), description (optional string), color (string hex), deptHead (optional string), sortOrder (number), createdAt (string ISO), updatedAt (string ISO)
    
    Follow exact field ordering and optional markers used in ARCHITECTURE.md section 1a.
  </action>
  <verify>
    <automated>grep -n "export const TABLE_DEPARTMENTS" src/lib/db/tables.ts</automated>
  </verify>
  <done>
    - TABLE_DEPARTMENTS constant exported with value 'departments'
    - DepartmentEntity interface extends TableEntity
    - All 8 required fields present with correct types: partitionKey 'department', rowKey, name, description?, color, deptHead?, sortOrder, createdAt, updatedAt
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Department type to domain.ts and departmentId to Team</name>
  <files>src/lib/types/domain.ts</files>
  <read_first>
    - src/lib/types/domain.ts (read Team interface at line 20)
    - ARCHITECTURE.md section 1b (Department and Team schema specs)
  </read_first>
  <action>
    Add Department interface before Team interface (around line 20). Fields: id (string), name (string), description (optional string), color (string), deptHead (optional string), sortOrder (number).
    
    Modify existing Team interface to add departmentId?: string field at the end, before the closing brace.
  </action>
  <verify>
    <automated>grep -n "export interface Department" src/lib/types/domain.ts && grep "departmentId" src/lib/types/domain.ts</automated>
  </verify>
  <done>
    - Department type exported with 6 fields matching DepartmentEntity schema (minus partitionKey/rowKey)
    - Team type includes departmentId?: string as optional field
    - No other Team fields modified
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Database to API | Table entities deserialized into domain types; optional fields must remain optional |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | TeamEntity deserialization | mitigate | departmentId field is optional; existing rows without it deserialize correctly as undefined |
| T-01-02 | Spoofing | DepartmentEntity.color field | mitigate | color is user-controlled free text (hex string); validate in API routes (Phase 2), not in schema |
| T-01-03 | Repudiation | createdAt/updatedAt timestamps | mitigate | generated server-side in API layer (Phase 2); not user input |
</threat_model>

<verification>
After Wave 1 execution:
- Schema types compile without error
- No circular dependencies between tables.ts and domain.ts
- Existing code using Team type still compiles (departmentId is optional)
</verification>

<success_criteria>
- TABLE_DEPARTMENTS = 'departments' constant is exported
- DepartmentEntity is exported with 8 fields, partitionKey: 'department'
- Department type is exported with 6 fields (id, name, description?, color, deptHead?, sortOrder)
- Team type includes departmentId?: string and maintains backward compatibility (no breaking changes to existing fields)
- No TypeScript errors or import issues
</success_criteria>

<output>
Create `.planning/phases/01-production-hardening-schema-foundation/01-01-SUMMARY.md` when done
</output>
