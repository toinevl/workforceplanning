---
phase: 03-department-management-ui
plan: 04
type: execute
wave: 4
depends_on: [03-01, 03-03]
files_modified:
  - src/components/teams/TeamFormModal.tsx
  - src/components/departments/BulkMigrateButton.tsx
  - src/app/settings/page.tsx
autonomous: true
requirements: [ASSIGN-01, ASSIGN-03]
must_haves:
  truths:
    - "TeamFormModal includes a department dropdown select field with \"Unassigned\" option"
    - "Department dropdown shows all departments from useDepartmentList"
    - "Department dropdown allows setting departmentId to undefined (unassigned state)"
    - "BulkMigrateButton is disabled when unassignedTeamCount is 0"
    - "BulkMigrateButton opens confirmation dialog with department dropdown on click"
    - "Confirmation dialog shows all departments from useDepartmentList for selection"
    - "BulkMigrateButton calls POST /api/admin/migrate-departments with selected departmentId"
    - "After successful migration, BulkMigrateButton shows success message and disables (one-time action)"
    - "Settings page renders BulkMigrateButton with unassignedTeamCount prop"
    - "Data state established: teams with undefined departmentId (for Phase 4 ASSIGN-02 bucket display)"
  artifacts:
    - path: "src/components/teams/TeamFormModal.tsx"
      contains: "useDepartmentList", "<select", "departmentId", "Unassigned", "value=\"\""
    - path: "src/components/departments/BulkMigrateButton.tsx"
      contains: "interface BulkMigrateButtonProps", "unassignedTeamCount", "useMigrateDepartments", "ConfirmDialog", "useDepartmentList"
    - path: "src/app/settings/page.tsx"
      contains: "import BulkMigrateButton", "<BulkMigrateButton", "unassignedTeamCount"
  key_links:
    - "TeamFormModal → useDepartmentList (fetch departments for dropdown options)"
    - "BulkMigrateButton → useMigrateDepartments (POST /api/admin/migrate-departments)"
    - "BulkMigrateButton → useDepartmentList (fetch departments for selection dialog)"
    - "Settings page → BulkMigrateButton (renders button with unassignedTeamCount prop)"
    - "Data state: teams with undefined departmentId (required for Phase 4 ASSIGN-02 Unassigned bucket)"
---

<objective>
Add department assignment capability to team edit form and create bulk migration button for unassigned teams in Settings.

Purpose: Enable users to assign teams to departments via Settings and bulk-migrate all unassigned teams at once. Establishes data state (teams with undefined departmentId) required for Phase 4's Unassigned bucket display.
Output: Updated team form with department dropdown, BulkMigrateButton component, and Settings page integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/hooks/useDepartments.ts
@src/lib/types/domain.ts
@src/app/settings/page.tsx
@.planning/phases/03-department-management-ui/03-01-SUMMARY.md
@.planning/phases/03-department-management-ui/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add department dropdown to team edit form in Settings</name>
  <files>src/components/teams/TeamFormModal.tsx</files>
  <read_first>
    - src/lib/hooks/useDepartments.ts (useDepartmentList hook)
    - src/lib/types/domain.ts (Team interface with departmentId field)
    - src/components/departments/DepartmentForm.tsx (reference form pattern from Phase 3 work)
  </read_first>
  <action>
Create src/components/teams/TeamFormModal.tsx as a new client component for team editing in Settings with department assignment:

1. Component structure: export a modal dialog component that wraps the team form
2. Import useDepartmentList hook at top
3. In the form fields section, add a new select field for department:
   - Label: "Department"
   - Type: dropdown (`<select>`)
   - Options: "Unassigned" (value="") as first option, then all departments from useDepartmentList (sorted by sortOrder)
   - Current value: controlled by form state (team.departmentId or "")
   - onChange: updates form state with selected departmentId (empty string if "Unassigned")
4. Styling:
   - Use same border and focus styling as other form inputs
   - Include loading state: show spinner if useDepartmentList.isLoading
   - If useDepartmentList.error, show error message below select
5. Form submission:
   - Include departmentId (or undefined if empty string) in the team update payload
   - API will handle undefined vs "" appropriately (optional field)

IMPORTANT: The "Unassigned" option (value="") establishes the data state required for ASSIGN-02 (Phase 4 will display unassigned teams in a bucket on the departments listing page). Do NOT modify any other form fields or behavior.
  </action>
  <verify>
    <automated>grep -c "useDepartmentList\|departmentId\|<select" /home/toine/projects/workforceplanning/src/components/teams/TeamFormModal.tsx | grep -q [2-9]</automated>
  </verify>
  <acceptance_criteria>
    - src/components/teams/TeamFormModal.tsx exists and is a client component
    - Component imports useDepartmentList
    - Team form includes a department dropdown select field
    - Dropdown shows "Unassigned" as default option (value="")
    - All departments from useDepartmentList appear as options with their names
    - Dropdown is controlled by form state (reflects team.departmentId or "")
    - onChange updates form state with selected departmentId
    - Dropdown includes loading and error states
    - Form submission includes departmentId in payload (undefined if empty string selected)
    - "Unassigned" option allows setting departmentId to undefined (data state for Phase 4 ASSIGN-02)
  </acceptance_criteria>
  <done>Team form modal created with department dropdown field that supports unassigned state</done>
</task>

<task type="auto">
  <name>Task 2: Create BulkMigrateButton component for one-time team migration</name>
  <files>src/components/departments/BulkMigrateButton.tsx</files>
  <read_first>
    - src/lib/hooks/useDepartments.ts (useMigrateDepartments hook)
    - src/components/ui/ConfirmDialog.tsx (confirmation dialog pattern)
    - src/lib/types/domain.ts (Department interface)
  </read_first>
  <action>
Create src/components/departments/BulkMigrateButton.tsx as a client component with:

Props interface:
```
interface BulkMigrateButtonProps {
  unassignedTeamCount: number;  // Count of teams with departmentId === undefined
}
```

Functionality:
1. Render a button labeled "Assign All Unassigned Teams"
   - Only enabled if unassignedTeamCount > 0
   - If count is 0, button is disabled with gray styling and tooltip "No unassigned teams"

2. On click:
   - Open a confirmation dialog:
     - Title: "Assign Unassigned Teams?"
     - Description: "This will assign X unassigned teams to the default department. This can only be done once."
     - Show a dropdown of all departments (fetched from useDepartmentList)
     - Label: "Select target department:"
     - Default: first department in list (or a "Default" department if it exists)

3. On confirm (user clicks "Assign"):
   - Call useMigrateDepartments(selectedDeptId) mutation
   - Show loading state: "Assigning..." in button
   - If success: close dialog, show success message "Successfully assigned X teams", disable button (migration is one-time)
   - If error: keep dialog open, show error message in red box with the error from API response

4. Dialog behavior:
   - Cancel button: close dialog without calling mutation
   - Escape key: close dialog (unless pending)

5. Styling:
   - Use consistent button styling from ConfirmDialog and DepartmentsSection
   - Department dropdown in dialog uses same styling as TeamFormModal select

6. Use 'use client' directive at top

Note: "Assign All Unassigned Teams" is intentionally one-time (no UI to "undo" or run again) — this enforces the ROADMAP requirement that migration is special and non-repeatable.
  </action>
  <verify>
    <automated>grep -c "useMigrateDepartments\|unassignedTeamCount\|ConfirmDialog\|useDepartmentList" /home/toine/projects/workforceplanning/src/components/departments/BulkMigrateButton.tsx | grep -q [3-9]</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/departments/BulkMigrateButton.tsx exists
    - Component accepts BulkMigrateButtonProps (unassignedTeamCount)
    - Button is disabled when unassignedTeamCount === 0
    - On click, opens confirmation dialog with department dropdown
    - Dialog allows selection of target department from useDepartmentList
    - On confirm, calls useMigrateDepartments(selectedDeptId)
    - Shows loading state "Assigning..." during mutation
    - Shows success message after successful migration
    - Shows error message (in red) on migration failure
    - Dialog stays open on error, allowing retry or cancel
    - Component uses 'use client' directive
  </acceptance_criteria>
  <done>BulkMigrateButton component created for one-time team migration</done>
</task>

<task type="auto">
  <name>Task 3: Integrate BulkMigrateButton into Settings page</name>
  <files>src/app/settings/page.tsx</files>
  <read_first>
    - src/app/settings/page.tsx (current Settings page)
    - src/components/departments/BulkMigrateButton.tsx (component just created)
    - src/lib/hooks/useDepartments.ts (useDepartmentList to get current teams)
  </read_first>
  <action>
Update src/app/settings/page.tsx to include BulkMigrateButton:

1. Import BulkMigrateButton and useDepartmentList at the top of the component
2. Fetch departments list using useDepartmentList hook to calculate unassignedTeamCount:
   - From useDepartmentList, extract all departments
   - Count teams with departmentId === undefined
   - Pass this count to BulkMigrateButton as prop
3. Add BulkMigrateButton below DepartmentsSection:
   - Create a new section labeled "Bulk Team Assignment" or similar
   - Include descriptive text: "Assign all unassigned teams to a default department. This action is permanent."
   - Place BulkMigrateButton in this section
4. Use consistent spacing and styling with other Settings sections

No other changes to Settings page.
  </action>
  <verify>
    <automated>grep -c "BulkMigrateButton\|unassignedTeamCount" /home/toine/projects/workforceplanning/src/app/settings/page.tsx | grep -q 1</automated>
  </verify>
  <acceptance_criteria>
    - Settings page imports BulkMigrateButton component
    - BulkMigrateButton is rendered in Settings page
    - unassignedTeamCount is calculated and passed as prop
    - New section for bulk migration appears after DepartmentsSection
    - Descriptive text explains the bulk migration action
    - Settings page styling and layout remain consistent
  </acceptance_criteria>
  <done>Settings page updated with BulkMigrateButton</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Department Dropdown → Migration API | Selected department ID from dialog crosses trust boundary; server validates it exists |
| Migration API Response → UI State | Success/failure response updates button state and shows user feedback |
| Team Form → API Patch | Department assignment in team form; server validates departmentId exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Injection | BulkMigrateButton department selection | mitigate | Department ID sourced from useDepartmentList query (API data); user selects from dropdown |
| T-03-20 | Tampering | Migration confirmation count | mitigate | Count displayed in dialog is from useDepartmentList query (fresh from API); user confirms before proceeding |
| T-03-21 | Repudiation | Bulk migration action | accept | Single-user app; no audit trail needed |
| T-03-22 | Information Disclosure | Migration error messages | mitigate | Only API error text displayed; no system info exposed |
| T-03-23 | DoS | Multiple migration clicks | mitigate | Button disabled after success; mutation prevents simultaneous requests |
| T-03-24 | EOP | Invalid departmentId in dropdown | mitigate | Department ID must exist in useDepartmentList; server validates before update |
</threat_model>

<verification>
Manual verification:
1. Navigate to /settings
2. Confirm "Bulk Team Assignment" section appears with BulkMigrateButton below DepartmentsSection
3. If no unassigned teams exist: verify button is disabled with gray styling
4. If unassigned teams exist: click "Assign All Unassigned Teams"
5. Verify confirmation dialog opens with department dropdown
6. Select a department and click "Assign"
7. Verify "Successfully assigned X teams" message appears
8. Verify button becomes disabled (one-time action)
9. Test team edit form: open a team for editing, verify department dropdown appears
10. Select a different department, save, verify team is updated in list
11. Verify form allows selecting "Unassigned" option to leave team without department assignment
</verification>

<success_criteria>
- TeamFormModal created with department dropdown field
- Dropdown includes "Unassigned" option (value="") that allows undefined departmentId
- BulkMigrateButton component created and functional
- Settings page includes BulkMigrateButton with unassignedTeamCount prop
- Button is disabled when no unassigned teams
- Confirmation dialog shows department dropdown with all departments
- Migration calls POST /api/admin/migrate-departments endpoint
- Success message displays after migration
- Button is disabled after successful migration (one-time action)
- Error messages display clearly on failure
- Team form assignment updates teams correctly
- Data state (teams with undefined departmentId) established for Phase 4 ASSIGN-02 listing feature
</success_criteria>

<output>
Create `.planning/phases/03-department-management-ui/03-04-SUMMARY.md` when done
</output>
