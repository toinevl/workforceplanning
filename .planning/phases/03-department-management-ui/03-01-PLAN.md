---
phase: 03-department-management-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/hooks/useDepartments.ts
autonomous: true
requirements: [DEPT-01, DEPT-02, DEPT-03, DEPT-04]
---

<objective>
Create TanStack Query hooks for department CRUD operations following established patterns from useScenario and useTeamBoard hooks.

Purpose: Provide React component libraries with data fetching and mutation hooks for all department operations before building the Settings UI.
Output: useDepartments.ts exported with useQuery and useMutation hooks for list, create, update, delete, and bulk migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/lib/hooks/useScenario.ts
@src/lib/types/domain.ts
@src/lib/utils/fetchJSON.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useDepartments hooks file with TanStack Query patterns</name>
  <files>src/lib/hooks/useDepartments.ts</files>
  <read_first>
    - src/lib/hooks/useScenario.ts (reference for useQuery and useMutation patterns)
    - src/lib/hooks/useTeamBoard.ts (reference for mutation cache invalidation)
    - src/lib/types/domain.ts (Department interface and types)
    - src/lib/utils/fetchJSON.ts (HTTP utility for API calls)
  </read_first>
  <action>
Create src/lib/hooks/useDepartments.ts with the following hooks, using identical patterns from useScenario.ts:

1. **useDepartmentList()** — useQuery hook:
   - queryKey: ['departments']
   - queryFn: calls fetchJSON('/api/departments') and returns the data array
   - Returns full Department array with rollup stats (headcount, FTE, team count from Phase 2 response)

2. **useDepartment(deptId)** — useQuery hook:
   - queryKey: ['department', deptId]
   - enabled: !!deptId
   - queryFn: calls fetchJSON(`/api/departments/${deptId}`)
   - Returns single Department object

3. **useCreateDepartment()** — useMutation hook:
   - mutationFn: (body: { name: string; color: string; description?: string; deptHead?: string }) =>
     fetchJSON<Department>('/api/departments', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) })
   - onSuccess: calls qc.invalidateQueries({ queryKey: ['departments'] })

4. **useUpdateDepartment()** — useMutation hook:
   - mutationFn: (args: { id: string; updates: Partial<{ name: string; color: string; description?: string; deptHead?: string }> }) =>
     fetchJSON<Department>(`/api/departments/${args.id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(args.updates) })
   - onSuccess: calls qc.invalidateQueries({ queryKey: ['departments'] })

5. **useDeleteDepartment()** — useMutation hook:
   - mutationFn: (id: string) =>
     fetch(`/api/departments/${id}`, { method: 'DELETE' }).then(async (r) => {
       const json = await r.json().catch(() => ({}));
       if (!r.ok) {
         // Extract assignedTeamCount from 409 response for error handling
         throw new Error(JSON.stringify({ error: json.error, assignedTeamCount: json.assignedTeamCount }));
       }
     })
   - onSuccess: calls qc.invalidateQueries({ queryKey: ['departments'] })

6. **useMigrateDepartments()** — useMutation hook:
   - mutationFn: (departmentId: string) =>
     fetch('/api/admin/migrate-departments', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ departmentId }) }).then(async (r) => {
       const json = await r.json().catch(() => ({}));
       if (!r.ok) throw new Error(json.error ?? 'Migration failed');
       return json.data;
     })
   - onSuccess: calls qc.invalidateQueries({ queryKey: ['departments'] })

All hooks must use 'use client' at the top of the file, import useQueryClient from @tanstack/react-query, and follow the exact export names above.
  </action>
  <verify>
    <automated>grep -c "useDepartmentList\|useCreateDepartment\|useUpdateDepartment\|useDeleteDepartment\|useMigrateDepartments" /home/toine/projects/workforceplanning/src/lib/hooks/useDepartments.ts | grep -q [5-9] && echo "All 5 hooks exported"</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/hooks/useDepartments.ts exists
    - All 6 hooks (useDepartmentList, useDepartment, useCreateDepartment, useUpdateDepartment, useDeleteDepartment, useMigrateDepartments) are exported
    - Each hook uses correct TanStack Query patterns (useQuery/useMutation, queryKey, queryFn, onSuccess with invalidateQueries)
    - useDeleteDepartment throws error with JSON.stringify including assignedTeamCount from 409 response
    - useMigrateDepartments calls POST /api/admin/migrate-departments endpoint
    - File starts with 'use client' directive
  </acceptance_criteria>
  <done>useDepartments.ts created with all 6 hooks following established patterns</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Component → API Route | Department data from user input crosses here; requires validation at route handler |
| API Response → Component State | TanStack Query caches all department responses; cache invalidation prevents stale data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | useCreateDepartment mutation | mitigate | Route handler validates name and color are non-empty strings; fetchJSON uses JSON serialization (not string interpolation) |
| T-03-02 | Tampering | useDepartmentList query cache | accept | TanStack Query cache is in-process memory; user only sees their own queries |
| T-03-03 | Repudiation | useMigrateDepartments mutation | accept | Single-user app, no audit trail required for v2.0 |
| T-03-04 | Information Disclosure | useDeleteDepartment error | mitigate | Error response includes only assignedTeamCount (integer count), no PII from teams |
| T-03-05 | DoS | Hook rate limiting | accept | Single-user app; no rate limiting needed |
| T-03-06 | EOP | Hook authorization | accept | Single-user app, no auth model in v2.0 |
</threat_model>

<verification>
Automated test:
```bash
npm test -- --testPathPattern=useDepartments 2>/dev/null || echo "No tests yet; manual verification of hook structure"
```

Manual verification:
1. Open src/lib/hooks/useDepartments.ts
2. Confirm all 6 hooks are exported and use correct query/mutation signatures
3. Spot-check one hook against useScenario.ts pattern match
</verification>

<success_criteria>
- useDepartments.ts exists and exports all 6 hooks
- Each hook uses TanStack Query correctly (useQuery/useMutation)
- Query keys follow ['departments'] and ['department', id] pattern
- Mutations invalidate ['departments'] query on success
- useDeleteDepartment extracts assignedTeamCount from 409 response
- File is executable without syntax errors
</success_criteria>

<output>
Create `.planning/phases/03-department-management-ui/03-01-SUMMARY.md` when done
</output>
