---
phase: 03-department-management-ui
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/departments/ColorPicker.tsx
  - src/components/departments/DepartmentForm.tsx
autonomous: true
requirements: [DEPT-01, DEPT-02]
must_haves:
  truths:
    - "ColorPicker component renders 10 Tailwind color swatches in a grid"
    - "Each swatch is clickable and shows visual selection indicator when selected"
    - "ColorPicker includes native <input type=\"color\"> below swatches for custom color fallback"
    - "ColorPicker onChange fires when either swatches or native picker changes"
    - "DepartmentForm has name, color, description, and deptHead input fields"
    - "DepartmentForm has mode-specific submit button (\"Create Department\" vs \"Save Changes\")"
    - "DepartmentForm has Cancel button that calls onCancel when clicked"
    - "DepartmentForm displays error message in red box when error prop is set"
  artifacts:
    - path: "src/components/departments/ColorPicker.tsx"
      contains: "interface ColorPickerProps", "ef4444", "f97316", "eab308", "22c55e", "06b6d4", "0ea5e9", "3b82f6", "8b5cf6", "ec4899", "6366f1", "input type=\"color\""
    - path: "src/components/departments/DepartmentForm.tsx"
      contains: "interface DepartmentFormProps", "useState", "name", "color", "description", "deptHead", "ColorPicker"
  key_links:
    - "DepartmentForm → ColorPicker (uses for color field in create/edit form)"
    - "DepartmentForm → onSubmit callback (called with form data when submit button clicked)"
---

<objective>
Create reusable form components for department creation and editing with a custom color picker using Tailwind palette + native color fallback.

Purpose: Provide UI components that Settings page will consume for department CRUD operations.
Output: ColorPicker component with 10 Tailwind swatches + fallback, and DepartmentForm controlled component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/types/domain.ts
@src/components/ui/ConfirmDialog.tsx
@src/components/ui/SectionLabel.tsx
@.planning/phases/03-department-management-ui/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ColorPicker component with Tailwind swatches and native fallback</name>
  <files>src/components/departments/ColorPicker.tsx</files>
  <read_first>
    - src/components/ui/SectionLabel.tsx (reference for label styling)
    - src/lib/types/domain.ts (Department interface confirms color: string field)
  </read_first>
  <action>
Create src/components/departments/ColorPicker.tsx as a controlled React component with:

Props interface:
```
interface ColorPickerProps {
  value: string;
  onChange: (color: string) => void;
  label?: string;
}
```

Functionality:
1. Display 10 Tailwind color swatches in a grid. Use this exact palette (all hex values):
   - #ef4444 (red-500)
   - #f97316 (orange-500)
   - #eab308 (yellow-500)
   - #22c55e (green-500)
   - #06b6d4 (cyan-500)
   - #0ea5e9 (sky-500)
   - #3b82f6 (blue-500)
   - #8b5cf6 (violet-500)
   - #ec4899 (pink-500)
   - #6366f1 (indigo-500)

2. Each swatch is a clickable button with:
   - Background color matching the hex value
   - A checkmark icon (or simple border highlight) visible when swatch is selected (value === hex)
   - Accessible ARIA labels like "Red (#ef4444)"

3. Below swatches, include a native `<input type="color">` with label "Custom color" for fallback/precision
   - This input controls the same `value` state
   - onChange fires when either swatches or native picker changes

4. Wrap with optional label at top using SectionLabel if provided

5. Use 'use client' directive at top of file
6. Use Tailwind CSS for styling (rounded buttons, grid layout)

Do NOT add any external color picker library — use native HTML color input only for fallback.
  </action>
  <verify>
    <automated>grep -E "ef4444|f97316|eab308|22c55e|06b6d4|0ea5e9|3b82f6|8b5cf6|ec4899|6366f1" /home/toine/projects/workforceplanning/src/components/departments/ColorPicker.tsx | wc -l | grep -q 10</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/departments/ColorPicker.tsx exists
    - Component exports ColorPickerProps interface and default ColorPicker component
    - All 10 Tailwind hex colors are defined in the component
    - Swatches render in a grid layout with visual selection indicator (border or checkmark)
    - Native color input is present below swatches
    - Both swatches and native input fire onChange callback with hex value
    - Component uses 'use client' directive
  </acceptance_criteria>
  <done>ColorPicker component created with 10 Tailwind swatches and native color input fallback</done>
</task>

<task type="auto">
  <name>Task 2: Create DepartmentForm component for create and edit modes</name>
  <files>src/components/departments/DepartmentForm.tsx</files>
  <read_first>
    - src/lib/types/domain.ts (Department interface)
    - src/components/departments/ColorPicker.tsx (ColorPickerProps to import)
    - src/components/ui/SectionLabel.tsx (label styling)
  </read_first>
  <action>
Create src/components/departments/DepartmentForm.tsx as a controlled form component with:

Props interface:
```
interface DepartmentFormProps {
  mode: 'create' | 'edit';
  initialData?: Partial<Department>;
  isLoading?: boolean;
  error?: string | null;
  onSubmit: (data: { name: string; color: string; description?: string; deptHead?: string }) => void;
  onCancel?: () => void;
}
```

Functionality:
1. Controlled form with useState for four fields: name, color (required), description, deptHead
2. Initialize from initialData if provided (edit mode); empty strings for create mode
3. Form fields in order:
   - Name (required, text input with placeholder "e.g., Engineering")
   - Color picker (required, use ColorPicker component, default to blue #3b82f6)
   - Description (optional, textarea with placeholder "Team charter, mission, notes...")
   - Department Head (optional, text input with placeholder "e.g., Jane Smith")
4. Submit button:
   - Label "Create Department" in create mode, "Save Changes" in edit mode
   - Disabled when isLoading === true or name is empty
   - Shows "Working..." text when isLoading === true
5. Cancel button (if onCancel provided):
   - Label "Cancel"
   - Disabled when isLoading === true
6. Error display:
   - If error prop is truthy, display in red box above form fields
7. Form styling:
   - Use Tailwind classes for consistent spacing (gap-4, padding)
   - All inputs have consistent border styles and focus rings
   - Layout is single column, full width
8. Use 'use client' directive at top

On form submission:
- Call onSubmit with { name, color, description (if not empty), deptHead (if not empty) }
- Do NOT call preventDefault — let onSubmit handler manage async behavior
  </action>
  <verify>
    <automated>grep -c "useState\|name\|color\|description\|deptHead\|onSubmit" /home/toine/projects/workforceplanning/src/components/departments/DepartmentForm.tsx | grep -q [5-9]</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/departments/DepartmentForm.tsx exists
    - Component accepts DepartmentFormProps (mode, initialData, isLoading, error, onSubmit, onCancel)
    - Form has four input fields: name (required), color (via ColorPicker, required), description (optional), deptHead (optional)
    - Submit button is disabled when isLoading or name is empty
    - Submit button text is mode-specific ("Create Department" vs "Save Changes")
    - Cancel button is present and disabled during loading
    - Error message displays in red box if error prop is set
    - ColorPicker is imported and used correctly
    - Component uses 'use client' directive
  </acceptance_criteria>
  <done>DepartmentForm component created for both create and edit modes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Input → Form State | Color and department fields come from user; form validates format, no injection |
| ColorPicker Value → API | Hex color string passed to onSubmit callback; validated at route handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Injection | DepartmentForm name/description fields | mitigate | Form inputs are plain text; JSON.stringify in API caller sanitizes; API route revalidates |
| T-03-08 | Tampering | ColorPicker swatch selection | accept | Client-side UI control; server validates color is hex string format |
| T-03-09 | Repudiation | Form submission audit | accept | Single-user app, no audit trail needed |
| T-03-10 | Information Disclosure | Form error display | mitigate | Error message shows only API error text; no stack traces or system info |
| T-03-11 | DoS | Large description field | accept | No size limits enforced; browser default form field behavior |
| T-03-12 | EOP | Form validation bypass | mitigate | Form UI validates required fields; server-side validation at API route rejects invalid input |
</threat_model>

<verification>
Manual verification:
1. Open src/components/departments/ColorPicker.tsx
   - Visually confirm all 10 color swatches render and are clickable
   - Test native color input changes the selected swatch
2. Open src/components/departments/DepartmentForm.tsx
   - Verify create mode shows "Create Department" button
   - Verify edit mode with initialData populates all fields
   - Verify Cancel button is present and functional
   - Verify error message displays when error prop is set
</verification>

<success_criteria>
- Both components created in src/components/departments/ directory
- ColorPicker renders 10 Tailwind swatches with selection indicator
- ColorPicker native color input is functional
- DepartmentForm is a controlled component with all required fields
- Form has mode-specific button labels
- Cancel button is present with loading state
- Error display is styled consistently
- Both components use 'use client' directive
</success_criteria>

<output>
Create `.planning/phases/03-department-management-ui/03-02-SUMMARY.md` when done
</output>
